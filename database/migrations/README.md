# database/migrations

Status: **implemented (Step 3).** The real, ledgered, re-runnable
application domain schema — applied via [dbmate](https://github.com/amacneil/dbmate)
(`docker compose run --rm migrate up`, wrapped by `scripts/db-migrate.sh`).
See
[docs/architecture/database-architecture.md](../../docs/architecture/database-architecture.md)
for the full schema, an ER diagram, the role/permission model, and why
dbmate was chosen over the alternatives.

16 migration files, numbered by timestamp, each with `-- migrate:up` and
`-- migrate:down` sections:

| File | Contents |
|---|---|
| `20260722190000_extensions_and_helpers.sql` | Shared trigger functions (`set_updated_at`, `assert_valid_transition`) and the `jsonb_has_no_secret_keys` guard |
| `20260722190001_channels.sql` | `channels` + 9 config tables |
| `20260722190002_content_lifecycle.sql` | `content_projects`, topic lifecycle, `content_briefs` |
| `20260722190003_research.sql` | `sources`, `research_claims`, `research_claim_sources` |
| `20260722190004_scripts.sql` | `scripts`, `script_versions` |
| `20260722190005_media_production.sql` | voiceovers, assets, scene manifests, render jobs, thumbnails, metadata variants |
| `20260722190006_approval_publication.sql` | `approval_requests`, `published_videos` |
| `20260722190007_analytics.sql` | `analytics_snapshots`, `strategy_insights` |
| `20260722190008_workflow_execution.sql` | `workflow_runs`, `workflow_steps`, `errors`, `dead_letter_jobs` |
| `20260722190009_prompts.sql` | `prompts`, `prompt_versions`, `channel_prompt_assignments` |
| `20260722190010_cost_accounting.sql` | `cost_events`, `provider_usage_events` |
| `20260722190011_audit_logs.sql` | `audit_logs` |
| `20260722190012_budget_functions.sql` | Live budget-calculation SQL functions |
| `20260722190013_job_claiming_and_resume.sql` | `SKIP LOCKED` job claiming + resume-logic SQL functions |
| `20260722190014_infra_healthcheck.sql` | `_infra.healthcheck` (carried over from Step 2's now-retired `docker-entrypoint-initdb.d` approach) |
| `20260722190015_grants.sql` | Explicit belt-and-suspenders grants on top of the bootstrap-time default privileges |

`database/schema.sql`, alongside this directory, is a dbmate-generated
plain-SQL snapshot of the current schema — regenerated by every
`scripts/db-migrate.sh` run, committed to git as a reviewable-in-diff
reference (not a backup mechanism — see database-architecture.md's
backup/restore section for that).

Applied as the `migrator` role — never the Postgres superuser, never the
runtime application role. See `database/bootstrap/README.md` for how
`migrator` itself is created.
