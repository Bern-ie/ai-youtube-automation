{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.ai-youtube-automation.internal/workflow-completion.schema.json",
  "title": "Complete Workflow Run — Request",
  "description": "Input to the 'Complete Workflow Run' shared n8n workflow. See complete_workflow_run() in database/migrations/20260722200000_workflow_runtime_functions.sql.",
  "type": "object",
  "additionalProperties": false,
  "required": ["workflow_run_id", "channel_id"],
  "properties": {
    "workflow_run_id": { "type": "string", "format": "uuid" },
    "channel_id": { "type": "string", "format": "uuid" },
    "output": { "type": "object" }
  }
}
